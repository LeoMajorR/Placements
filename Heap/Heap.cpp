#include <bits/stdc++.h>
using namespace std;

// heapify
void heapify(vector<int> &arr, int i)
{
    int size = arr.size();
    int largest = i;

    // left and right child
    int l = 2 * i + 1;
    int r = 2 * i + 2;
}

// function to insert elements into heap
void insert(vector<int> &arr, int data)
{
}